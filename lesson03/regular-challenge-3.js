// Use a for loop to console log all of the elements plus the index and a comma
// Example:
//
// // 1,cats
// // 2,dogs
//
// ..and so on

let a = ['batman','spiderman','ironman','hulk','thor','captain'];
for (i = 0; i < a.length; i++) {
  console.log(i+','+a[i]);
}
